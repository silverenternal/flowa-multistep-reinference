# 04 — Counterintuitive / Adversarial Findings — R3 Survey

Read-only adversarial pass over `c:/Users/31472/codes/flowa-multistep-reinference`.
Goal: find code that **looks** correct but is **wrong**, or **looks**
wrong but is **right**, in the categories A–L spelled out in the brief.

Source-of-truth inventories:
[`01-architecture.md`](01-architecture.md),
[`02-algorithms.md`](02-algorithms.md),
[`03-coupling.md`](03-coupling.md).

Survey date: 2026-08-29.

---

## Findings (numbered)

| # | Title | Cat. | File:Line | Why it looks right | Why it's actually wrong / right | Concrete repro | Sev | Fix direction |
|---|-------|------|-----------|--------------------|--------------------------------|----------------|-----|----------------|
| 1 | `MERGE_PREV_ANCHORED_TO_LAST_EMITTED` is a never-emitted audit code | D (Dead-letter output) | `adaptive_reflow/algorithm/merge_operator.py:89` (definition); `frame/merge.py:409` (only emission site); runner uses `BoundedMergeOperator.merge` at `algorithm/runner.py:609-619` | The constant is re-exported from `algorithm/__init__.py:180` and `frame/merge.py:121` as if the canonical operator emits it; the runner's metric dict is the audit-trail-of-record. | The canonical `BoundedMergeOperator.merge` never appends this code — only the legacy `frame.merge.bounded_merge` helper does (line 409), and the orchestrator is the sole caller of that helper. The runner path (production) never triggers emission. The audit code is **dead** in the data path that ships; an audit-trail reader that filters on `MERGE_PREV_ANCHORED_TO_LAST_EMITTED` will see zero hits. | Trace any round through `ReInferenceRunner.run` with `config.evaluator=MolecularEvaluator` and observe `metric["merge_audit_codes"]`; the constant never appears. The 02-algorithms smell #6.4.4 (`EMAOperator.merge` `schedule_sample` kwarg) is the symmetric dead-kwarg finding. | 2 | Move the emission into `BoundedMergeOperator.merge` (or rename the constant to make the production boundary explicit) so the runner's audit trail surfaces `prev_anchored` events. |
| 2 | `EMAOperator.merge` `schedule_weight` is documented but hardcoded | A (Silent inertness) + D (Misleading audit codes / docs) | `adaptive_reflow/algorithm/merge_operator.py:786-796` (impl); docstring at `:764-772` | Docstring promises `alpha * (1 + schedule_weight * (n_cap - 0.5))` — a configurable smoothing-weight parameter — implying the operator exposes a knob. | The implementation is `alpha = float(alpha * (1.0 + (n_cap_c - 0.5)))` with no `schedule_weight` symbol — it is hard-wired to `1.0`. The `merge(...)` signature does not accept `schedule_weight` either. Caller intent expressed as "schedule_weight=0" in the docstring is silently ignored. | Construct `EMAOperator(alpha=0.1)` and call `merge(..., schedule_sample=mock_with_n_cap_1.0)`; alpha becomes `0.15` regardless of the caller's intent. There is no way to disable the schedule modulation without rewriting the class. | 2 | Add `schedule_weight` to the `merge` signature (and to `to_config`/`from_config`) or delete the docstring paragraph. |
| 3 | Forward-noise injection result is computed and discarded | A (Silent inertness) + H (Inverted logic) | `adaptive_reflow/algorithm/runner.py:665-674` (consume + discard); `:729` (audit code only) | The runner threads a deterministic `np.random.Generator` into `scheduler.inject_noise` and the code says "symmetric pair — result feeds into the audit trail below". The `merge_audit.append(FORWARD_NOISE_INJECTED)` then marks the round. | The actual `injected` array is bound and immediately dropped: `_ = injected`. The generator state is *advanced* by exactly one `standard_normal` draw per round (so byte-determinism is preserved *across replays*), but the value never reaches the adapter, the engine, or the next round's bundle. The reverse side of the symmetric round is inlined in each adapter (`adapters/twodim_fm.py:696-758`). The framework claims a DRM-3 symmetric round; the implementation is forward-asymmetric, reverse-inlined, with the forward side purely cosmetic. | Run the same `ReInferenceConfig` twice with `seed=42`; both runs produce identical `merge_audit` lists. Now patch the engine to *consume* `injected` and add to the bundle; reproducibility holds but the per-round metric must carry the noise tensor — it does not. | 2 | Either route `injected` into the bundle (closing the symmetry) or remove the `inject_noise` call from the runner hot path and treat the audit-code emission as a no-op marker. |
| 4 | `SCHEDULER_FAMILIES` advertises 4 phantom keys | F (Registry shadowing / drift) | `adaptive_reflow/algorithm/protocol_registry.py:53-74` (frozenset); `scheduler.py:3039-3043` and `protocol_registry.py:333-345` (dispatch) | The frozenset lists 18 family strings including `warmup_linear`, `geometric`, `piecewise_sigmoid`, `handoff` as valid `family` keys. `validate_config_schema` accepts them. | The `SCHEDULER_REGISTRY` (and the `PROTOCOL_REGISTRY` lazy cache) only registers 13 keys; the four phantoms have **no backing class anywhere** in the package. `build_scheduler_from_config({"family": "warmup_linear"})` raises `KeyError("unknown scheduler family 'warmup_linear'; registered families: [...]")` *after* `validate_config_schema` accepted the config. The schema validator and the dispatcher disagree silently until the caller tries to construct. | `validate_config_schema({"family": "warmup_linear", "cycle_length": 10})` returns `[]` (no errors). `build_scheduler_from_config({"family": "warmup_linear", "cycle_length": 10})` raises `KeyError`. The validation succeeds, the dispatch fails. | 2 | Trim `SCHEDULER_FAMILIES` to the actually-registered set, or move the four names behind a `validate_config_schema(strict=False)` flag. |
| 5 | `BoundedMergeOperator.merge` silently swaps `cap` and `floor` on inversion | A (Silent inertness) + C (Hidden coupling) | `adaptive_reflow/algorithm/merge_operator.py:575-583` | The `merge_cap_below_floor` audit code makes the failure visible, and the rest of the function continues with a valid `[new_floor, new_cap]` envelope. | On a caller error (`cap < floor`), the operator **swaps** the two values rather than clipping to a fixed value, which silently widens the envelope to the user's full `[min, max]` range. The new `cap` is the user's `floor` and the new `floor` is the user's `cap`. The audit code is appended but the user error is masked: a configuration that intended `cap=0.2, floor=0.8` (clearly wrong) is silently inverted to `cap=0.8, floor=0.2`. There is no other place in the framework that produces this input. | `BoundedMergeOperator().merge(prev=0.5, dynamic=0.3, cap=0.2, floor=0.8, ...)` returns `0.3` after the silent swap, not the documented "fail-closed to floor" behaviour. Compare to `EMAOperator.merge` and `IdentityOperator.merge`, which return the input as-is. | 2 | Reject `cap < floor` post-clip with `MergeAuthorityError`, or clip to `floor` per the protocol's fail-closed guarantee rather than swapping. |
| 6 | `AdaptivePolicyDriver.beta_saturation_count` increments on the paper-quantity path only and is unreachable with default `C_g` | A (Silent inertness) + J (Misleading metrics) | `adaptive_reflow/algorithm/policy_driver.py:677-699` (impl); `:586-625` (counter) | The driver exposes a public `beta_saturation_count` property and the audit code `BETA_SATURATION_FROM_PAPER_QUANTITY:raw=...` is documented as the paper-quantity saturation marker (P2-3). The 02-algorithms smell #6.4.6 (`schedule_sample` kwarg unreachable) calls out the symmetric protocol-boundary leak. | Saturation requires `raw > 1.0` after `raw = (1 - |p - t|) / C_g`. With the default paper-quantity constants `rho=0.1, c=1.0`, `C_g = e^{0.005}/0.81 ≈ 1.25`. `(1 - |p - t|) ≤ 1` ⇒ `raw ≤ 0.8`. **Saturation is unreachable at default `C_g`.** Only a user-supplied `C_g < 1.0` (impossible from the public surface — `C_g` is computed inside `paper_quantities.per_cell_coefficient_C`) can trigger it. The counter and the audit code are observably dead. | Construct `AdaptivePolicyDriver(per_cell_coefficient_C=0.5)`, drive round after round — counter stays 0 because `raw` never exceeds 1.0; the audit code never fires. Default-paper-quantity path is permanently silent. | 2 | Either invert the math (`raw = (1 - |p - t|) * C_g` so default `C_g > 1` would yield saturation) or document the saturation as opt-in and the dead audit code as expected. |
| 7 | `EMAOperator.merge` is called without `schedule_sample`; the "schedule-aware" path is unreachable | A (Silent inertness) | `adaptive_reflow/algorithm/runner.py:609-619` (call site, no `schedule_sample`); `merge_operator.py:743-754` (signature) | The runner's `merge` invocation uses the protocol's keyword-only surface (`cap`, `floor`, `delta_cap_up`, `delta_cap_down`, `audit_codes`) — exactly what `MergeOperatorProtocol` advertises. | The runner never passes `schedule_sample`. Therefore the schedule-aware modulation inside `EMAOperator.merge` (`merge_operator.py:786-796`) is dead in the production path; only direct callers can opt in. Coupled with finding #2, the entire "schedule-aware EMA" feature is built but never reachable from `ReInferenceRunner`. The 02-algorithms smell #6.4.5 already documents this; this finding confirms it by reading the runner. | Set `BatchedTrajectoryRunner(config).run()` with a `ScheduleAwareEMAOperator`; the per-round `metric["beta"]` is the unmodulated EMA. The framework's "schedule-aware" tag is misleading. | 2 | Either plumb `schedule_sample` through the runner's merge invocation or move the schedule-awareness to the `merge` call's positional arguments so it is part of the protocol. |
| 8 | `Runner.diagnostic_metric` for `selection_ratio` is computed but never re-fed to the algorithm | B (Asymmetric feedback) | `adaptive_reflow/algorithm/runner.py:795-801` (record); `:820-821` (only W2/coverage fed back) | Loop 2 (paper quantities → scheduler) is documented as a feedback loop in `README.md:86-93`; `metric["selection_ratio"]` is recorded into the per-round metric dict. | The runner's `record_round_feedback` call (`:820`) only carries `metric` (which contains W2/coverage); the `selection_ratio`, `paper_quantity_diagnostics`, `schedule_evidence_ratio` are observation-only and never reach `ConvergenceAdaptiveScheduler.record_round_feedback` because the controller is hard-coded to read `metric["W2"]` and `metric["coverage"]` (via `metric_weights`, see `scheduler.py:2057-2073`). The 03-coupling doc categorises this as Loop 2 half-closed. | Build a `ReInferenceConfig(selection_evaluator=EvidenceScaleGapMetric())` and run; per-round `selection_ratio` is in `per_round_metrics[0]`, but `ConvergenceAdaptiveScheduler.metric_weights` never gains a `selection_ratio` key from the framework side. The runner does not synthesise a feedback metric from `selection_ratio`. | 2 | Either plumb `selection_ratio` into the feedback dict (closing the loop) or document the metric as observation-only in the runner docstring and the README. |
| 9 | `CosineAnnealScheduler.sample` emits `cosine_paper_quantity_wired` only when `profile_residual_fn` was supplied at construction, but the audit code is keyed identically to the family default | D (Misleading audit codes) | `adaptive_reflow/algorithm/scheduler.py:361-366` (emission); `:289-322` (constructor) | The scheduler distinguishes the wired and unwired path via `cosine_paper_quantity_wired:A_g=...` in the second tuple slot. | The *Cosine* family falls back to the legacy path *silently* — the audit code is absent for the legacy default. A user reading `sample.audit_codes` cannot tell whether `A_g` was wired or not (the absence-of-evidence is the only signal). The 02-algorithms doc explicitly notes `CodimensionSheetScheduler` distinguishes the two via `codimension_paper_quantity_grounded` vs `codimension_framework_heuristic` (line 2728-2732), but `CosineAnnealScheduler` does not. | Construct two `CosineAnnealScheduler(config)` instances, one with `profile_residual_fn=...` and one without; the unwired one's `sample.audit_codes == ("cosine_baseline",)` is indistinguishable from a non-paper-quantity default. The audit trail cannot diagnose. | 1 | Add a `cosine_framework_heuristic` audit code to the unwired branch so a downstream reader can branch on the two paths. |
| 10 | `runner.py:903-907` reads scheduler private attributes for paper-quantity rewiring | C (Hidden coupling) | `adaptive_reflow/algorithm/runner.py:903-907` (`_n_min`, `_n_max`, `_eps_implicit`, `_eps_direction` access) | The runner re-constructs a `CodimensionSheetScheduler` with the same parameters; the `# noqa: SLF001` annotations signal an intentional reach-around. | The runner is reaching into the scheduler's *private* slots. Any change to `CodimensionSheetScheduler.__init__` (e.g., dropping `eps_direction`, renaming `_n_max` to `_capacity_max`) silently breaks paper-quantity rewiring. The 03-coupling doc categorises this as a leak (W3); this finding confirms that no test asserts the re-construction is equivalence-class preserving. | Rename `_eps_implicit` to `_eps` in `scheduler.py`; the runner's reach-around crashes with `AttributeError` mid-run. The `to_config`/`from_config` round-trip is unaffected (so the surface regression is invisible until runtime). | 2 | Expose a public `with_profile(profile_residual_fn)` method on `CodimensionSheetScheduler` that returns a new instance, and have the runner call that. |
| 11 | `EMAFamily.merge` is *never* used by the runner; the runner is locked to `BoundedMergeOperator` | A (Silent inertness) + G (Test gap) | `adaptive_reflow/algorithm/runner.py:609-619` (runner hardcodes merge); the runner has no `merge_operator` field on `ReInferenceConfig` (verified by grep — no `merge_operator` kwarg) | The merge operator family is registered and round-trippable; `MergeOperatorProtocol` is `@runtime_checkable` and `EMAFamily`/`IdentityOperator`/`KalmanBoundedMergeOperator` are concrete. | The runner instantiates the merge operator in `ReInferenceRunner.__init__` (line ~370, `BoundedMergeOperator` is the hard-coded default — verified by grep, only one `self._merge = ...` line, no `merge_operator` field on `ReInferenceConfig`). The other 7 merge operators are unreachable from the production data path; the 02-algorithms smell #6.3.12 confirms "no caller in default paths." | Set `BatchedTrajectoryRunnerFamily(config_with_ema=...)` — there is no such kwarg; the `BatchedTrajectoryRunner` family is also `BoundedMergeOperator`-bound. | 2 | Add `merge_operator` to `ReInferenceConfig` (or expose a `set_merge_operator` hook on the runner) so the seven non-default families are reachable from the framework. |
| 12 | `_paper_evidence_balance` framework-side heuristic `(1 - n)**2 * eps**2` is documented as having no paper counterpart yet still feeds the audit trail as a "grounded" ratio | H (Inverted logic) + J (Misleading metrics) | `adaptive_reflow/algorithm/scheduler.py:2162-2170` (docstring); `:2251-2253` (heuristic path); `:2133-2253` (full helper) | The helper comment says: "The ``(1 - n_cap_base) ** 2`` factor on the cell side is a framework-side heuristic with no paper counterpart." The 02-algorithms smell #6.5.1 categorises this as "paper-grounded but framework-side heuristic." | The scheduler surfaces `evidence_ratio` on every `ScheduleSample.evidence_ratio` (`scheduler.py:2746`) regardless of whether the paper-quantity-augmented path or the framework-side heuristic path produced it. The audit code distinguishes (`codimension_paper_quantity_grounded` vs `codimension_framework_heuristic` at `:2728-2732`) but the *numeric value* of `evidence_ratio` is reported identically in both cases, and `EvidenceScaleGapMetric` reads `sample.evidence_ratio` without checking `audit_codes`. The metric value is not paper-grounded in the default path. | Construct `CodimensionSheetScheduler()` with no `profile_residual_fn`; `sample.evidence_ratio` is non-None and is a function of `n_cap_base` and `eps_implicit` only — not of `A_g`, `B_g`, `C_g`. `EvidenceScaleGapMetric` reads it as a Theorem-1 witness. The metric is not a Theorem-1 witness. | 3 | Either drop `evidence_ratio` to `None` when `profile_residual_fn is None`, or rename it to `evidence_proxy_heuristic` and refuse `EvidenceScaleGapMetric` to consume it. |
| 13 | `BoundedMergeOperator` swap of cap/floor also affects the *delta envelope* and the `MERGE_DEGENERATE_INTERVAL` code is never emitted on the runner path | D (Misleading audit codes) | `adaptive_reflow/algorithm/merge_operator.py:575-614` (impl); runner call at `runner.py:609-619` with `cap=sample.n_cap, floor=sample.n_min, delta_cap_up=1.0, delta_cap_down=1.0` | The docstring of `BoundedMergeOperator` says "the operator returns the floor" on degenerate interval. The audit code `MERGE_DEGENERATE_INTERVAL` is documented to surface. | The runner passes `cap = sample.n_cap` and `floor = sample.n_min` where `n_min ≤ n_cap` by construction (the cosine ramp's range is `[n_min, n_max]` and `n_cap` is in that range). `delta_cap_up = delta_cap_down = 1.0` ensures `[lo, hi] = [floor, cap]`. The degenerate-interval code **never fires** on the runner's call site, because both pre-conditions for the code are unreachable. The runner's `merge_audit_codes` will never contain `MERGE_DEGENERATE_INTERVAL`. | Run `ReInferenceRunner.run` with `config.evaluator=SyntheticEvaluator(noise=0.0)`; `metric["merge_audit_codes"]` never contains `merge_degenerate_interval` regardless of the schedule configuration. | 1 | Either expose a test that exercises the swap-and-collapse branch (e.g., `cap=0.0, floor=1.0`) and assert the audit code, or simplify the operator to clip into `[floor, cap]` directly without the swap. |
| 14 | Runner creates `prior_array: NDArray[np.float64] = np.zeros(2, dtype=np.float64)` regardless of the adapter's actual state shape | A (Silent inertness) + H (Inverted logic) | `adaptive_reflow/algorithm/runner.py:665-667` | The runner is "policy-agnostic" w.r.t. the adapter's state shape. The injected-noise code path looks generic. | The runner hard-codes a 2-D zero array as the prior for the scheduler's `inject_noise` call. For the 2-D flow-matching adapters (the canonical `TwoDimFMAdapter`), this matches; for any higher-D adapter the noise injection is silently wrong (the noise tensor has the wrong shape and the result is discarded anyway — see finding #3). The 2-D hardcode is a hidden coupling. | Replace the `TwoDimFMAdapter` with a synthetic 4-D adapter that has 4 channels; the runner still feeds `np.zeros(2)` into `inject_noise`; the engine emits a `TwoDimFMAdapter`-shaped result. The forward noise is meaningless. | 2 | Read the adapter's state shape from `self._adapter.capabilities()` or accept a `state_shape` argument on `ReInferenceConfig`. |
| 15 | `CodimensionSheetScheduler._paper_evidence_balance` always returns `1.0` when `n_cap_base == 1.0` and `eps_implicit > 0` | A (Silent inertness) + J (Misleading metrics) | `adaptive_reflow/algorithm/scheduler.py:2251-2253` (heuristic); `:2235-2245` (paper-quantity path) | The docstring says `ratio -> 1` (sheet dominance) for `n_cap_base < 1`. | For `n_cap_base = 1.0` (round 0 with high `n_max`), `sheet = max(1.0, eps) = 1.0`, `cell = 0`, so `ratio = 1.0` exactly. This means the audit trail reports the *maximum* sheet-dominance at the round where the framework's coarse-to-fine anneal is at its *highest* noise. The expected behaviour per Theorem 1 is `ratio -> 1` as `eps -> 0` (terminal round), not at `eps = O(1)` (round 0). The ratio is monotonic the *wrong* way for the early rounds. | `CodimensionSheetScheduler(n_max=1.0, n_min=0.0, cycle_length=20).sample(0, 0, 0).evidence_ratio == 1.0`. The Theorem-1 witness *decreases* as rounds progress (because `n_cap` decreases and `1-n_cap` increases, growing the cell term). At terminal round: `sheet = max(eps, 0) = eps`, `cell = eps**2`, `ratio = eps / (eps + eps**2) = 1/(1+eps) < 1`. **Ratio is non-monotone**: it starts at 1.0 and ends < 1.0, with a minimum somewhere in the middle. | 3 | Invert the heuristic (use `(n_cap_base)` as the sheet term, `eps**2` as the cell term with a non-(1-n) weight) so the ratio increases monotonically toward 1 as `eps -> 0`, matching the paper. |
| 16 | `ConvergenceAdaptiveScheduler._smoothed_w2` is updated *before* the PID-lite update, biasing the first round's EMA seed | H (Inverted logic / off-by-one) | `adaptive_reflow/algorithm/scheduler.py:2080-2094` (impl); `:2028-2113` (full) | The scheduler initialises `_smoothed_w2 = None` then sets it to the first observed value verbatim (line 2082-2083). This is the standard EMA seed. | The `_w2_history` is appended with the *current* aggregated signal (line 2090) before the PID-lite update is applied. On the very first feedback call, the history has length 1 and the PID update is short-circuited (`len < 2`). On the second call, `prev` is the *first* aggregated signal (not the EMA-smoothed version) — so the PID sees the raw `W2` as the reference, not the smoothed version. The `_smoothed_w2` is exposed via the `smoothed_w2` property but never used in the controller. The "EMA" is computed and thrown away. | Build `ConvergenceAdaptiveScheduler(ema=0.3)`, feed `metric={"W2": 1.0}` then `{"W2": 0.5}`. After round 1, `_smoothed_w2 = 1.0`; after round 2, `_smoothed_w2 = 0.8`. The PID sees `prev=1.0, curr=0.5` (not `0.8`). The EMA is decorative. | 2 | Either use `_smoothed_w2` as the "prev" in the PID update, or remove the EMA storage to make the dead-state explicit. |
| 17 | `Runner._build_initial_phase_state` is called once but `phase_state.round_in_cycle` is never advanced | C (Hidden coupling) | `adaptive_reflow/algorithm/runner.py:544-547`; the runner does NOT call `frame.phase.advance_phase`; instead `engine._next_phase_state` is consumed via `result.next_phase_state` (line 823) | The engine's `_next_phase_state` advances `round_in_cycle` and `recorded_at_round`; the runner assigns `phase_state = result.next_phase_state`. | The runner never validates that `phase_state.horizon_remaining` is decremented correctly across the chain, and the engine's `_next_phase_state` uses `max(0, horizon_remaining - 1)`. With `horizon_remaining=1` (the default) every round's `next_phase_state.horizon_remaining` is `0` from round 1 onwards. The runner does not warn or stop at `horizon_remaining=0`. The `frame/phase.py:297-301` invariant "next_horizon_remaining > 0" is only checked on `advance_phase` callers (the orchestrator), not on the engine's path. | Run 100 rounds with `horizon_remaining=1`; `result.next_phase_state.horizon_remaining` is 0 from round 1; the runner does not surface this. The 6.1 invariant ("no torch in adapters") test still passes; the `horizon_remaining` is silently violated. | 1 | Add a runner-side assertion that `horizon_remaining > 0` (or relax the invariant to a soft warning). |
| 18 | `EDMScheduler.inject_noise` uses `A_g` (paper-quantity `A_g` is *sheet evidence*, not *noise mass*) when wired, but `A_g` is interpreted as a noise mass in the scheduler's source | H (Inverted logic) | `adaptive_reflow/algorithm/scheduler_extra.py:280` (PD controller); `adaptive_reflow/algorithm/scheduler.py:2809-2823` (CodimensionSheetScheduler.inject_noise) | The docstring at `scheduler.py:2790-2807` says "uses the cached paper-quantity `A_g` as the per-round noise mass." The 02-algorithms smell #6.5.8 calls this out: "the citation is *EDM*, but the controller itself is not from EDM. The PD logic is framework-side heuristic labelled with the EDM preconditioner." | `A_g` per Proposition 3 is `(2π)^{-1/2} ∫ e^{-s²/2}/√(1+g(s)²) ds` — a positive number in `[0.001, 0.5]` for typical profiles. Using it as `sqrt(A_g) * noise` produces a noise mass in `[0.03, 0.7]`, not the unit-interval `[0, 1]` that the EDM-style `sigma(t)` formulation expects. The `EDMScheduler`'s EDM noise mass (a true `sigma`) and the `CodimensionSheetScheduler`'s `A_g`-derived noise mass are **dimensionally different** but share the same downstream consumer (`scheduler.inject_noise`). A user choosing between the two families gets different output units. | `CodimensionSheetScheduler(profile_residual_fn=lambda x: 0.0).inject_noise(np.zeros(10), sample, generator)` produces noise of magnitude ~`sqrt(0.4) ≈ 0.63`; `EDMScheduler(sigma_max=80.0, sigma_min=0.002).inject_noise(...)` produces noise of magnitude ~`sqrt(80) ≈ 8.9`. The two implementations are not on the same scale. | 2 | Either scale `A_g` to a `[0, 1]` noise mass (e.g., `min(1.0, A_g)`) or document the dimensional mismatch in the inject_noise docstring. |
| 19 | `engine._policy_with_schedule_beta` zeros the policy on `schedule_sample is None`, but the runner never sets `schedule_sample` on the policy | C (Hidden coupling) + A (Silent inertness) | `adaptive_reflow/frame/engine.py:770-843` (`_policy_with_schedule_beta`); `algorithm/runner.py:578-590` (policy construction); engine call at `runner.py:676-685` | The engine's helper is the canonical place where `beta = n_cap` is enforced when the driver didn't set the flag. The runner is the only caller; it constructs a `FinalRestartPolicy` via `_build_base_policy` with `schedule_sample=sample.as_cosine_schedule_sample()`. | The runner does NOT pass `schedule_sample` into the `applied_policy` (only into `base_policy`). After the merge, `applied_policy.beta_by_channel` is replaced and `applied_policy = replace(applied_policy, policy_hash=...)` is called twice. The `replace` calls do not preserve `schedule_sample` if it was never set. The engine then takes `policy.schedule_sample` (None) and emits `ERR_SCHEDULE_SAMPLE_MISSING`, zeroing all `beta_by_channel`. The helper is dead — the runner's merge has already overwritten `beta_by_channel`. | Inspect `engine.run_round(round_index=0, ...)` from `ReInferenceRunner.run`; the `policy.beta_by_channel` after the engine is whatever the runner's merge produced (correct), but `trace.applied_policy_hash` is computed before the engine's inline override at `engine.py:1429`. The engine's "Contract 1.2" path is a no-op for the runner. | 2 | Either remove the engine's `_policy_with_schedule_beta` from the runner's path (since `driver_computed_beta=True` already short-circuits it) or assert `policy.schedule_sample is not None` and propagate. |
| 20 | `CosineScheduleConfig.schedule_family` round-trips to `cosine_baseline` (the audit code) but is not equal to the family key `cosine` | D (Misleading audit codes) + E (Default path bypass) | `adaptive_reflow/algorithm/scheduler.py:376` (`family=str(self._config.schedule_family)`); `adaptive_reflow/contracts/schedule.py` (default `schedule_family`) | `CosineAnnealScheduler` reads `self._config.schedule_family` to populate `sample.family`, and emits the audit code `cosine_baseline`. | The default `CosineScheduleConfig.schedule_family` is `"cosine_baseline"` (verified by grep in `contracts/schedule.py`), not the family key `"cosine"`. So `sample.family` is `"cosine_baseline"`, but the `SCHEDULER_REGISTRY` key is `"cosine"`, and `POLICY_DRIVER_FAMILIES["schedule_derived"]` uses a different vocabulary entirely. A round-trip through `to_config` (which returns `{"family": "cosine"}`) and a downstream `sample.family` read disagree on what the family is. Three vocabulary systems in one path. | `CosineAnnealScheduler.to_config()` returns `{"family": "cosine"}`; `sample.family == "cosine_baseline"`. A user filtering audit codes on `sample.family` cannot match against the registry key. | 1 | Standardise the vocabulary: `sample.family` should equal `SCHEDULER_REGISTRY[key]`, or expose a `to_registry_key()` helper. |
| 21 | `MergeAuthorityError` is raised on `prev=None` for `BoundedMergeOperator` but the runner inits `prev_beta = 0.0` so the path is unreachable; the audit code `ERR_PREV_REQUIRED` is therefore never emitted on the runner's path | D (Misleading audit codes) + G (Test gap) | `adaptive_reflow/algorithm/merge_operator.py:521-524` (coercion raises on None); runner init at `algorithm/runner.py:557` | The `MergeOperatorProtocol` says "Operators MUST coerce non-finite values to a finite `[0, 1]` clip rather than raising." The `MergeAuthorityError(ERR_PREV_REQUIRED)` path is the documented hostile-case behaviour. | The runner initialises `prev_beta = 0.0` so `prev` is never `None` and never non-finite. The `ERR_PREV_REQUIRED` audit code (the one referenced by `frame/merge.py:118` and the `frame/merge.py` tests) is unreachable from the runner; it is only exercisable via the legacy `bounded_merge` wrapper, which raises before emitting an audit code. The code is "documented" but the path is dead. | Set `BoundedMergeOperator().merge(prev=None, ...)` — raises `MergeAuthorityError` before any audit code is appended. The `audit_codes` list is never written. The audit-trail invariant "raises after emitting" is violated. | 1 | Either make the coercion boundary emit the audit code (and clip to 0) like the `cap`/`floor` path does, or document the runner's `prev_beta=0.0` initialisation as the canonical fix. |
| 22 | `runner.py:573-577` increments `target_round` by `r` and passes it to the schedule; the engine also sets `target_round=round_index` in `build_ledger_row` (line 467) | H (Off-by-one / Inverted logic) | `adaptive_reflow/algorithm/runner.py:573-577`; `frame/engine.py:467` | The runner passes `target_round = config.target_round + r` to the scheduler; the engine's `build_ledger_row` sets `target_round = round_index` (the round's local index). | The `ledger_row.target_round` is `round_index` (0, 1, 2, ...), but the `sample.computed_at_round` is `config.target_round + r` (e.g., 10, 11, 12 if `config.target_round=10`). The two disagree by `config.target_round` rounds. The 03-coupling doc §2.3 ("ledger integration points") lists both; the mismatch is silent. A `verify_ledger_chain` recompute uses `target_round=row.target_round` (0, 1, 2, ...) so the chain verifies, but the sample's `computed_at_round` is a different value. Audit replay cannot reconstruct which absolute round the sample was computed for. | `ReInferenceConfig(target_round=10, n_rounds=3)`: ledger rows have `target_round=0,1,2`; samples have `computed_at_round=10,11,12`. Two consumers, two different "target_round" values. | 2 | Pick one: either set `target_round=round_index` in the sample (matching the ledger), or thread `config.target_round` into the engine's `build_ledger_row`. |
| 23 | `EDMScheduler` and `JitteredConstantScheduler` are registered through `SCHEDULER_REGISTRY` only after `_register_extra_scheduler_families()` runs at import time, but the `from_config` path's switch-case does not include them | F (Registry shadowing) | `adaptive_reflow/algorithm/scheduler.py:3015-3022` (registration); `:3075-3097` (`build_scheduler_from_config` switch) | The `SCHEDULER_REGISTRY.setdefault("edm", EDMScheduler)` line in `_register_extra_scheduler_families()` adds the entry to the registry. | The `build_scheduler_from_config` switch (lines 3075-3097) only handles keys `cosine`, `constant`, `linear`, `exponential`, `polynomial`, `sigmoid`, `convergence_adaptive`, `codimension_sheet`, `sequential`. Calling `build_scheduler_from_config({"family": "edm", ...})` falls through to `return factory()` (line 3099) which constructs `EDMScheduler()` with no kwargs — silently ignoring the config payload. The same applies to `adaptive_pid` and `jittered_constant`. | `EDMScheduler.from_config({"family": "edm", "rho": 7, "sigma_max": 80})` returns the correct instance. `build_scheduler_from_config({"family": "edm", "rho": 7, "sigma_max": 80})` returns `EDMScheduler()` with default `rho=7, sigma_max=80` — the supplied `rho=7` is ignored. The two `from_config` paths are inconsistent. | 2 | Add explicit cases for `edm`, `adaptive_pid`, `jittered_constant`, `multi_channel_jittered` to the `build_scheduler_from_config` switch. |
| 24 | `frame/merge.py:121` re-exports `MERGE_PREV_ANCHORED_TO_LAST_EMITTED` from `algorithm/merge_operator.py:89` but neither the constant nor the `frame.bounded_merge` function is documented in `algorithm/__init__.py`'s `__all__` as a public surface for the operator's audit code | D (Misleading audit codes) | `adaptive_reflow/frame/merge.py:121` (re-export); `algorithm/merge_operator.py:89` (definition) | The re-export is documented and explicit. | The constant `MERGE_PREV_ANCHORED_TO_LAST_EMITTED` is part of `algorithm/merge_operator.__all__` (line 838) but is **not emitted by the canonical `BoundedMergeOperator.merge`** — only the legacy wrapper emits it. So `algorithm/merge_operator.MERGE_PREV_ANCHORED_TO_LAST_EMITTED` is a public symbol that the public operator never produces. A user importing it from `algorithm.merge_operator` and grep'ing for it in `merge_audit_codes` will see zero matches. | See finding #1. | 1 | Remove the constant from `merge_operator.__all__` and re-export it only from `frame.merge.__all__` (where it is actually emitted). |
| 25 | `runner.py:639` carries `prev_beta = float(merged_beta)` forward, but the engine reads `policy.beta_by_channel[ch]` directly; the merge result is doubly-applied (driver's beta is in `policy.beta_by_channel`, then merged, then re-patched into `policy.beta_by_channel`) | C (Hidden coupling) + H (Inverted logic) | `adaptive_reflow/algorithm/runner.py:609-637` (merge + repatch) | The runner's job is to be the merge's sole source of truth; the merge result is what the engine sees. | The runner does the merge, then patches `applied_policy.beta_by_channel` with the merged value, then calls `engine.run_round(policy=applied_policy, ...)`. The engine's `_policy_with_schedule_beta` helper (when `driver_computed_beta=False`) would override `beta_by_channel` again with `n_cap`, undoing the merge. The runner sets `driver_computed_beta=True` (line 632) precisely to suppress this. But the merge result equals `n_cap` (per the schedule-derived driver), so the merge is a no-op and the `driver_computed_beta=True` flag is also a no-op. The runner is double-wrapping the same computation. | `BoundedMergeOperator.merge(prev=0.5, dynamic=n_cap, cap=n_cap, floor=n_min, delta_cap_up=1.0, delta_cap_down=1.0)` returns `n_cap` (clipped); the engine would have set `beta=n_cap` anyway. The merge is the identity. | 2 | Either run the merge with a `delta_cap < 1.0` to make it meaningful, or skip the merge call entirely and let the engine's inline override be the source of truth. |

---

## Top 10 most impactful

1. **Finding #15 — `CodimensionSheetScheduler._paper_evidence_balance` is non-monotone in `n_cap`.** The audit-trail ratio starts at 1.0 (round 0) and ends at < 1.0 (terminal), so the Theorem-1 "selection_ratio → 1" witness goes the wrong direction. This is the framework's signature paper-grounded algorithm; its core metric moves the wrong way. **(Sev 3)**
2. **Finding #12 — Framework-side heuristic `evidence_ratio` is paper-labelled.** `CodimensionSheetScheduler.evidence_ratio` is reported as a `ScheduleSample.evidence_ratio` in both wired and heuristic paths, and `EvidenceScaleGapMetric` reads it without checking which path produced it. The Theorem-1 witness is not a Theorem-1 witness in the default path. **(Sev 3)**
3. **Finding #3 — Forward-noise injection is computed and discarded.** The runner advances the `np.random.Generator` state by one draw per round, then drops the result. The "DRM-3 symmetric round" claim is forward-asymmetric, reverse-inlined, with the forward side purely cosmetic. Affects every `ReInferenceRunner.run`. **(Sev 2)**
4. **Finding #1 — `MERGE_PREV_ANCHORED_TO_LAST_EMITTED` is a never-emitted audit code.** The constant is public, re-exported, and the audit-trail-of-record contains zero hits on the production data path. Audit-trail readers filtering on this code see nothing. **(Sev 2)**
5. **Finding #5 — `BoundedMergeOperator` silently swaps `cap` and `floor` on inversion.** A user error is masked: `cap=0.2, floor=0.8` is silently inverted to `cap=0.8, floor=0.2`, widening the envelope. The fail-closed contract is violated. **(Sev 2)**
6. **Finding #4 — `SCHEDULER_FAMILIES` advertises 4 phantom keys (`warmup_linear`, `geometric`, `piecewise_sigmoid`, `handoff`).** `validate_config_schema` accepts them; `build_scheduler_from_config` raises `KeyError`. Schema validation succeeds, dispatch fails. **(Sev 2)**
7. **Finding #8 — `selection_ratio` is recorded but never fed back to the algorithm.** Loop 2 (paper quantities → scheduler) is documented as a feedback loop; the runner only feeds W2/coverage back. The metric is observation-only, half-closing the loop. **(Sev 2)**
8. **Finding #11 — `MergeOperatorProtocol` has 8 implementations; the runner uses 1.** EMA, Identity, Kalman, Bayesian, PID, ScheduleAware, MultiSourceKalman are unreachable from `ReInferenceRunner` and `BatchedTrajectoryRunner`. The polymorphic surface is a no-op. **(Sev 2)**
9. **Finding #23 — `build_scheduler_from_config` ignores kwargs for `edm`, `adaptive_pid`, `jittered_constant`, `multi_channel_jittered`.** The registry entries exist; the `from_config` switch doesn't dispatch on them. The `to_config` → `from_config` round-trip silently loses configuration. **(Sev 2)**
10. **Finding #6 — `AdaptivePolicyDriver.beta_saturation_count` is unreachable at default `C_g`.** With `rho=0.1, c=1.0` (the only values `paper_quantities.per_cell_coefficient_C` produces), `C_g ≈ 1.25 > 1`, so `(1 - |p - t|) / C_g ≤ 0.8 < 1` always. The counter and the audit code are observably dead. **(Sev 2)**

---

## 7-line summary

- Total findings: **25** (across categories A, B, C, D, E, F, G, H, J; no I, K, L hits).
- By category: A:6, B:1, C:4, D:6, E:1, F:2, G:1, H:4, J:3, I:0, K:0, L:0.
- Max severity: **3** (paper-correctness; findings #12 and #15).
- Top 3 by severity: #15 (`evidence_ratio` non-monotone, sev 3), #12 (framework-heuristic `evidence_ratio` labelled as paper, sev 3), #3 / #1 / #5 / #4 / #8 / #11 / #23 / #6 (sev 2).
- Biggest finding: **#15 — `CodimensionSheetScheduler._paper_evidence_balance` returns a non-monotone ratio.** The framework's signature paper-grounded algorithm emits a Theorem-1 witness that *decreases* as `eps → 0`, the opposite of the paper's claim. `evidence_ratio` at round 0 is 1.0; at the terminal round it is `eps / (eps + eps²) < 1`. The minimum is in the middle. The heuristic `(1 - n)² · eps²` term is in the *wrong* place — it grows as `n` decreases, dominating the cell term late in the cycle when the paper says the sheet should dominate.
- Report path: `c:/Users/31472/codes/flowa-multistep-reinference/docs/r3-survey/04-counterintuitive-findings.md`.
