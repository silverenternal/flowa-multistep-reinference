# Six untracked TODO plans: status reconciliation

The six root plans dated 2026-09-12 were cross-checked against `git log` and
their implementation/audit artifacts. Their headers previously said
`PLANNED`, although all five executable proposals had landed in Waves
124–126; the framework-vs-model document is their synthesis. Headers are now
marked `DONE` with commit or audit references. No plan files were deleted.

The remaining work is evidence maintenance and any long-running N=1000/N=5000
reruns; those are intentionally not started here. The Kanzi bridge audit
records the required resumable command and the current torch limitation.

Validation: six files retain their original bodies, only status/reference
lines changed; no external weights or GPU process was launched.

