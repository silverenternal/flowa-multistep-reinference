# Wave131 CIFAR reference build

`--build-ref` completed successfully in `.venvs/kanzi_venv`; the generated
`data/cifar10_test_ref.npz` contains 10,000 test images and is ~118 MiB.
SHA-256 and exact array shapes are recorded in
`verification_outputs/wave131_cifar_ref_build/metadata.json`. No paper claims
were changed.
