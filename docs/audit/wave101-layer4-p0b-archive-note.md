# Wave 101 Layer 4 P0-B audit

The planned archive-note task names `r4/`, `r5/`, and `r17-survey/`
directories. A repository scan found only `docs/r17-survey/`; no `r4/` or
`r5/` directory exists, so no placeholder directories were created. An
`ARCHIVED.md` note was added to the existing survey directory. This is an
additive documentation change and does not alter source code or experiment
configuration.

Validation: Markdown relative-link scan over `todo/` and `docs/` reports zero
broken links.

