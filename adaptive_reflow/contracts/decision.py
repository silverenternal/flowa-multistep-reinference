"""Adaptive reflow typed contracts — reserved for future DTB-R2/R8 decision shapes.

Currently empty; symbols are kept in :mod:`adaptive_reflow.contracts.bundle`
so the layout stays symmetric with the per-DTB files. Do not export
anything here until an actual addition lands.
"""
