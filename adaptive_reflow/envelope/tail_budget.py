"""Tail budget — reserved for future tail-budget-only moves.

Currently empty; :class:`TailBudgetAccumulator` and
:class:`StratifiedTailBudgetRow` remain in
:mod:`adaptive_reflow.envelope.manifest` because the two are tightly
coupled. The file is reserved so a future split can land without
shuffling the manifest's imports.
"""
