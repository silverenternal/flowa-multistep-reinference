"""Tests for the Wave 54 Phase 2 FlowMol3 baseline scripts.

This directory mirrors the Wave 52 Agent C convention (no tests
shipped for the Tier 1/2 baselines — the comparison JSON itself is
the unit-of-truth). The Wave 54 Phase 2 baselines are tier-3 with a
new shape, so a small focused test suite is added: byte-stable check,
helper unit-tests, and a smoke import.
"""
